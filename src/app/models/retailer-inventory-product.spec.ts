import { RetailerInventoryProduct } from './retailer-inventory-product';

describe('RetailerInventoryProduct', () => {
  it('should create an instance', () => {
    expect(new RetailerInventoryProduct()).toBeTruthy();
  });
});
